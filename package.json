{
  "name": "easy-qemu",
  "private": true,
  "version": "0.2.0",
  "description": "Easy QEMU — desktop manager for QEMU virtual machines",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6"
  }
}
