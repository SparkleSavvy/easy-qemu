{
  "name": "easy-qemu",
  "private": true,
  "version": "0.2.0",
  "description": "Root helper that forwards npm scripts into ui/",
  "scripts": {
    "tauri": "npm --prefix ui run tauri",
    "dev": "npm --prefix ui run tauri dev",
    "build": "npm --prefix ui run tauri build"
  }
}
